<h1>EAT DA BURGER!</h1>

<h2>Add you favorite Burger!</h2>

<ul>
  {{#each burgers}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Cats that are sleepy!</h2>

<ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Cat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Type:</label>
    <input type="text" id="burger_form" name="name">
  </div>

  <div class="form-group">
    <input type="input" name="burger_input" value="Enter a Burger">
  </div>

  <button type="submit">Add Burger</button>
</form>